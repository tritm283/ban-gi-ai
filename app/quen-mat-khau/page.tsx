"use client";
import { FormEvent,useState } from "react";import Link from "next/link";import { taoSupabaseTrinhDuyet } from "@/lib/supabase/client";
export default function Trang(){const [email,setEmail]=useState("");const [tb,setTb]=useState<string|null>(null);const [dang,setDang]=useState(false);const gui=async(e:FormEvent)=>{e.preventDefault();const s=taoSupabaseTrinhDuyet();if(!s)return setTb("Hệ thống đăng nhập chưa được cấu hình.");setDang(true);await s.auth.resetPasswordForEmail(email.trim().toLowerCase(),{redirectTo:`${window.location.origin}/xac-thuc?next=${encodeURIComponent("/dat-lai-mat-khau")}`});setDang(false);setTb("Nếu email tồn tại trong hệ thống, hướng dẫn đặt lại mật khẩu đã được gửi.");};return <main><div className="the-form"><h1>Quên mật khẩu</h1><p>Nhập email của bạn để nhận liên kết đặt lại mật khẩu.</p><form className="bieu-mau" onSubmit={gui}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="nut-chinh" disabled={dang}>{dang?"Đang gửi…":"Gửi hướng dẫn"}</button></form>{tb&&<div className="thong-bao">{tb}</div>}<p><Link href="/dang-nhap">Quay lại đăng nhập</Link></p></div></main>}
