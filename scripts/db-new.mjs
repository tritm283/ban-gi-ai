import fs from "node:fs";import path from "node:path";const raw=process.argv.slice(2).join(" ").trim();if(!raw){console.error('Cách dùng: npm run db:new -- "ten thay doi"');process.exit(1);}const slug=raw.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/đ/g,"d").replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");const d=new Date();const p=n=>String(n).padStart(2,"0");const version=`${d.getUTCFullYear()}${p(d.getUTCMonth()+1)}${p(d.getUTCDate())}${p(d.getUTCHours())}${p(d.getUTCMinutes())}${p(d.getUTCSeconds())}`;const file=`${version}_${slug}.sql`;const target=path.join(process.cwd(),"supabase","migrations",file);fs.writeFileSync(target,`-- ${raw}\n-- Mỗi thay đổi cơ sở dữ liệu phải nằm trong một migration mới.\n\n`);console.log(target);
