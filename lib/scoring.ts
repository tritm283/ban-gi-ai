import type { TinHieuSanPham } from "@/lib/types";
const TRONG_SO = { demand: .30, growth: .25, margin: .20, viral: .15, competition: .10 } as const;
function hopLe(v:number|null|undefined){return typeof v==="number"&&Number.isFinite(v)&&v>=0&&v<=100;}
export function tinhDoPhu(s:TinHieuSanPham){let tong=0;if(hopLe(s.demand))tong+=TRONG_SO.demand;if(hopLe(s.growth))tong+=TRONG_SO.growth;if(hopLe(s.margin))tong+=TRONG_SO.margin;if(hopLe(s.viral))tong+=TRONG_SO.viral;if(hopLe(s.competition))tong+=TRONG_SO.competition;return Math.round(tong*100);}
export function tinhDiemCoHoi(s:TinHieuSanPham){const doPhu=tinhDoPhu(s);if(!hopLe(s.demand)||!hopLe(s.growth)||doPhu<60)return {diem:null,doPhu,trangThai:"INSUFFICIENT_EVIDENCE" as const};let tong=0;let w=0;const cong=(v:number|null,wgt:number,dao=false)=>{if(hopLe(v)){tong+=(dao?100-v!:v!)*wgt;w+=wgt;}};cong(s.demand,TRONG_SO.demand);cong(s.growth,TRONG_SO.growth);cong(s.margin,TRONG_SO.margin);cong(s.viral,TRONG_SO.viral);cong(s.competition,TRONG_SO.competition,true);const nen=tong/w;const ruiRo=hopLe(s.risk_penalty)?s.risk_penalty!:0;const diem=Math.max(0,Math.min(100,Math.round((nen-ruiRo)*100)/100));const provisional=doPhu<100||!hopLe(s.risk_penalty);return {diem,doPhu,trangThai:provisional?"PROVISIONAL" as const:"READY" as const};}
export function tinhDiemXepHang(opportunity:number|null,confidence:number,coverage:number){if(opportunity===null)return -1;return opportunity*(.6+.4*confidence/100)*(.7+.3*coverage/100);}
export function nhanDiem(diem:number|null){if(diem===null)return "Chưa đủ dữ liệu";if(diem>=85)return "Cơ hội mạnh";if(diem>=75)return "Đáng cân nhắc thử";if(diem>=65)return "Cần kiểm tra thêm";return "Ưu tiên thấp";}
